sudo apt update
sudo apt install -y curl wget apt-transport-https ca-certificates software-properties-common
wget -qO - https://artifacts.elastic.co/GPG-KEY-elasticsearch \
  | sudo gpg --dearmor -o /usr/share/keyrings/elasticsearch-keyring.gpg
echo "deb [signed-by=/usr/share/keyrings/elasticsearch-keyring.gpg] https://artifacts.elastic.co/packages/9.x/apt stable main" \
  | sudo tee /etc/apt/sources.list.d/elastic-9.x.list
sudo apt update
sudo apt install elasticsearch=9.1.5
sudo systemctl enable elasticsearch.service --now
sudo systemctl status elasticsearch.service

sudo nano /etc/elasticsearch/elasticsearch.yml
xpack.security.enabled: false
xpack.security.http.ssl.enabled: false


curl -X DELETE "http://localhost:9200/bench"

curl -X GET "http://localhost:9200/bench/_mapping?pretty"